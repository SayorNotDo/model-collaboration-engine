# 仓库协作指南

## 项目与工作入口

本项目是供 Python 异步代理调用的 Rust 模型协作内核，负责模型路由、有界策略执行、预算账本和任务状态持久化。宿主负责事件循环与工具执行权限。

涉及领域术语、职责边界或概念关系时，阅读 [CONTEXTS.md](CONTEXTS.md)；术语含义变化时同步更新该文件。

修改前阅读 [README.md](README.md) 的执行约定和当前边界，并检查工作区已有变更。依赖、特性开关和 Python 打包配置分别以 [Cargo.toml](Cargo.toml)、[pyproject.toml](pyproject.toml) 为准。

## 任务路由

接到任务后，先按 [.agent/task-router.md](.agent/task-router.md) 识别任务类型，读取并执行对应工作流；混合任务按路由规则组合流程。代码入口与验收参考按需读取 [.agent/code-map.md](.agent/code-map.md)，验证命令统一见 [.agent/README.md](.agent/README.md)。

策略保持与供应商身份、传输协议解耦；协议适配放在 adapter，账本事务放在 store。测试执行编排时，优先通过 `Engine::with_components` 注入适配器和存储。

新增、修改或审查代码时，阅读并应用 [代码实现规范](.agent/coding-standards.md) 中受影响的模块、Rust、Python、持久化与验收规则。

## 必须保持的业务语义

- **规划与提交**：所有任务经 `run/stream` 的统一提交路径，是否规划由 planning 配置决定。规划在已登记任务内执行，无工具权限，候选池不能绕过数据约束；建议不能覆盖宿主明确类型/策略或削弱验收。规划和执行共用账本、调用次数、截止时间与关闭清理。结算/存储失败不回退，未知规划费用不因回退释放。原始提交、建议和有效计划分别保留；当前数据库结构内的提交与账务证据必须可恢复查询，不承诺跨 schema 兼容。

- **预算与调用次数**：费用使用整数 microcredits。模型调用、工具执行和模型续写共用 `max_calls`；调用前事务性预留预算并计数。已知 usage 或工具费用按实际值结算，未知费用保留预留金额；实际费用超出预留时先记账再报告错误。
- **取消与收尾**：调用开始后的取消、超时和失败路径需要完成相应账务处理与终态写入。Python 取消及提前退出流后继续等待原生结果与回调清理；重复取消仍需保留清理保护和取消信号。
- **引擎关闭**：停止准入后先等待任务完成，再取消并等待清理；仅在活动任务排空后关闭数据库。清理超时保留数据库所有权与重试关闭能力，Python 关闭取消保护需覆盖宿主回调清理。
- **任务身份与恢复**：同一数据库的 `task_id` 保持唯一。未知费用通过有证据的对账处理；新增恢复或重放逻辑时，明确重复调用和重复计费的处理方式。
- **路由与策略上限**：先执行能力、上下文、模型、供应商、地域和预算约束，再对可用候选评分。策略变更同时检查调用额度、轮数和截止时间的终止路径。
- **类型画像**：所有执行都使用固定画像快照；缺失画像使用全局先验，不保留旧评分路径。规划阶段使用全局先验选择宿主候选池。模型版本/角色/验收版本隔离；缺失按确定性层级回退。计划与固定快照原子保存，画像质量评分和级联门槛使用相同 Q；任务内 Health 不能冒充跨任务质量统计，角色映射不能改变工具权限。
- **反馈与指标**：确定性检查、critic 结论与业务验收分层保存，completed 不自动产生质量样本；critic 正确性须由宿主独立评价。反馈按实际产物及调用归因，ID 幂等且拒绝冲突；不同版本、角色和反馈种类隔离。历史反馈只影响后续任务快照，调用状态/耗时与结算原子保存，未知费用不得视为零。
- **宿主权限**：通过 `ToolExecutor` 或 Python 显式注册的异步回调执行工具，宿主负责逐请求授权与完整参数校验。保持整批请求先检查、工具串行执行、失败不自动重试，以及评审节点不执行工具的语义；执行 ID 用于追踪，不构成跨任务 exactly-once 保证。
- **事件语义**：事件队列与单事件大小有界，背压受执行截止时间约束。候选内容增量和流结束均不代表成功，最终结果以任务终态及账本为准；区分实时内容增量、持久化编排事件与账本审计记录。
- **凭证**：供应商密钥通过配置指定的环境变量读取，示例与测试使用占位值或本地模拟服务。

## 跨层变更

发现文档与代码不一致时，先依据用户要求、已确认的设计及相关证据确定预期行为，再判断应修正文档还是实现。代码现状是核对证据，不自动取代设计意图；已有上下文足以判断时直接处理，只有影响后续执行的关键预期仍不明确时才向用户澄清。

修改 JSON 契约时，同步检查 Rust 校验、序列化、Python 字典接口、`examples/` 和相关测试。Python 公共接口保持独立于供应商 SDK 类型。

修改 SQLite schema 时，同步更新当前版本和建库定义；开发期不维护跨版本迁移链，版本不匹配时拒绝并提示备份重建，禁止自动清库。修改用户可见行为或功能边界时，同步更新根目录 README。

保留已有源码风格，避免为局部变更格式化无关代码。构建输出、虚拟环境、数据库和原生调试文件遵循 `.gitignore`。

## 验证与交付

根据任务路由选定的范围执行 [.agent/README.md](.agent/README.md) 中的验证流程。

技术图使用 archify，入口见 [技术图索引](docs/diagrams/README.md)，执行步骤见 [技术图工作流](.agent/workflows/diagram.md)。修改已有图覆盖的模块职责、跨层调用关系或关闭状态语义时，同步核对并更新受影响图及源码证据；规划中的能力必须与当前实现区分。JSON 是图的编辑源，不直接修改生成的 HTML。结构检查、浏览器检查与视觉审查分别报告，不能相互替代。

行为测试验证可观察结果、调用次数或账本状态；涉及预算、取消、超时和失败结算时覆盖对应异常路径。仅文档修改核对源码事实、文件引用和空白错误即可。

交付说明具体变化、实际运行的检查及未验证项。离线模拟测试与真实供应商联调分别报告；提交前检查暂存 diff，确保内容属于本次任务。

## 初期接口演进

当前接口尚在开发期，不默认承诺旧 API 或旧算法行为兼容。契约调整时同步迁移仓库调用方、示例和测试；保留预算、权限及结算不变量。纯模拟数据库可显式重建；真实调用费用及未结算预留保留到对账完成，必要复现样本选择性归档。数据库打开只接受当前 schema，不自动升级或删除文件；跨版本迁移在正式发布前按实际需要设计。
