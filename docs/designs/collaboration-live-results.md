# 复杂协作真实联调结果

日期：2026-09-16。通过 DeepSeek 官方 `chat_completions`，使用实际 Engine、四个独立新库和宿主只读回调运行。[完整报告](../../artifacts/collaboration-20260916-01/results/report.json) · [账本核对](../../artifacts/collaboration-20260916-01/audit.json) · [配置快照](../../artifacts/collaboration-20260916-01/config.json)。

## 实际发生的路径

| 场景 | 持久化证据 | 终态 / 最终答案 | 调用计数 | 配置价上界估算 |
| --- | --- | --- | ---: | ---: |
| 级联 | Flash 首版缺少标记被拒绝，Pro 第二版补齐 | completed / 通过 | 2 模型 | 0.007131 元 |
| 生成与评审 | Flash 首版 41，Pro 要求修订；Flash 改为 42，Pro 通过 | completed / 通过 | 4 模型 | 0.040905 元 |
| 规划 | Flash 规划建议、有效计划、Flash 执行 | completed / 通过 | 2 模型 | 0.008750 元 |
| 工具 | Flash 请求 lookup、宿主校验并返回固定值、Flash 续写 | completed / 通过 | 2 模型 + 1 工具 | 0.003826 元 |

10 次模型调用均保存供应商响应 ID 和 usage。每个任务的 `settled` 等于 attempts 的费用合计，`calls` 等于模型与工具尝试数，全部预留余额为零。只读工具实报费用为零，仍占一次共享调用额度。

合计 **60,612 microcredits（0.060612 元配置价上界估算）**。按高峰、输入缓存未命中价计算，单位为每人民币元 1,000,000 microcredits；尚未核对供应商实际扣费，不能把账本已结算解释为供应商账单已匹配。[价表核实与证据缺口](billing-precision.md)单独记录。

## 结论边界

级联和修订由合成目标故意制造第一版错误，证明调用、反馈、轮次、约束和账本链路能在真实服务上运行；不证明自然任务上的质量提升或成本优势。配置中的模型身份和版本由宿主提供，报告不独立证明供应商内部模型实现。

规划采用明确的 `required` 模式和 Flash 候选池，执行保留宿主指定 single 策略。工具为无外部副作用的本地只读回调，不代表复杂业务工具或其他供应商已验证。本轮未测试真实 Responses。

所有任务只运行一次，没有重试该实验或覆盖既有费用证据。原始 SQLite 文件在本地保留并由 `.gitignore` 排除；JSON 报告为脱敏后的可审查证据。本轮不将合成流程验收作为路由质量反馈样本写回。
