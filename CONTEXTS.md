# 模型协作领域上下文

本项目围绕一个领域：在明确的目标、验收条件和资源限制下，组织模型与宿主工具协作，产出可评价的结果。宿主保有工具授权与业务副作用的决定权。

本文统一领域术语与概念关系。接口用法及当前能力边界见 [README.md](README.md)，仓库协作规则见 [AGENTS.md](AGENTS.md)，开发任务的工作流选择见 [.agent/task-router.md](.agent/task-router.md)。

## 任务与参与方

**协作任务（Task）**：一次具有独立身份的工作目标，包含证据、验收条件、协作策略及资源限制。同一任务可以包含多轮协作和多次调用。
_避免混用_：一次模型请求、一个策略节点。

**宿主（Host）**：向引擎提交协作任务并提供工具执行能力的应用。宿主负责业务授权、完整工具参数校验和副作用管理。

**模型候选（Model Candidate）**：可供路由选择的模型配置身份，关联能力、质量先验、费用及可用性等信息。候选身份与供应商侧模型名称可以不同。

**证据（Evidence）**：任务提供的事实材料或来源信息，具有来源及信任属性。证据中的文本本身不授予执行权限。

## 选择与协作

**模型路由（Model Routing）**：在满足任务硬约束的候选中选择一次模型调用的执行者。
_避免混用_：开发任务路由、协作策略。

**协作策略（Strategy）**：组织生成、评价、升级或修订步骤的任务执行方式。策略规定协作关系，模型路由选择具体执行者。

**策略节点（Node）**：协作过程中承担一个明确职责的步骤，例如生成或评审。一个节点可以因工具续写或失败尝试而包含多次调用。

**协作轮次（Round）**：一次候选产物生成及其相关评价形成的策略迭代单位。
_避免混用_：模型调用次数、工具执行次数。

**调用尝试（Attempt）**：一次模型调用或一次宿主工具执行所对应的独立计数与记账单位。工具完成后的模型续写属于新的调用尝试。

**上下文快照（Snapshot）**：某次协作步骤使用的任务目标、验收条件、证据、既有产物与反馈的版本化集合。
_避免混用_：整个会话历史、恢复检查点。

## 提交与规划

**提交（Submission）**：宿主提供的不可变目标、验收、权限和资源限制，可省略任务类型或策略。它是所有新任务统一的入库与执行输入；TaskSpec 是显式任务值或有效计划的执行投影。

**规划建议（Planner Proposal）**：规划模型提出的类型、现有策略、能力与验收建议。建议没有工具权限，模型自报置信度不代表质量概率。

**有效计划（Effective Plan）**：确定性校验后形成的执行选择，包含类型、角色映射、约束、验收及版本证据。宿主硬约束不能放宽，明确选择不能被覆盖。

**规划阶段（Planning Phase）**：已准入任务中的受限模型调用，共用任务预算、调用次数、截止时间、取消和关闭清理。阶段上限只会收紧任务上限，不提供额外预算。

**任务类型（Task Type）**：目标类别；与 text/json/tools 调用能力正交。所有任务的类型和角色决定质量证据及评分权重的查询路径，不授予能力或权限。

**质量画像（Quality Profile）**：针对模型配置身份、模型版本、任务类型、角色和验收版本的宿主提供质量证据。缺失时沿父类型、general、全局先验回退，不将未知视为零。它不等于模型自报置信度或 critic 输出通过的频率。

**路由快照（Routing Snapshot）**：每个任务执行期间固定的候选配置、角色画像、权重及评分时刻。与每次调用的余额、排除项和临时健康状态一起支持路由复算；不同于上下文快照，也不代表自动重放或长期指标库。

## 工具与授权

**工具声明（Tool Specification）**：任务允许模型提出的工具名称、参数约定及单次费用上限。声明说明可请求的能力，具体业务操作仍由宿主授权。

**工具调用请求（Tool Call）**：模型提出的工具名称、参数与调用标识，等待宿主处理。
_避免混用_：工具已执行、操作已成功。

**工具执行身份（Execution ID）**：一次宿主工具执行的追踪和记账身份，关联提出请求的模型调用。它不承诺跨任务去重，也不保证外部副作用恰好发生一次。

**工具结果（Tool Result）**：宿主返回给模型的执行输出及已知或未知的实际费用。输出可以表达业务失败，收到结果不等于业务操作成功。

## 产物与评价

**候选产物（Artifact）**：由生成步骤产生、带有版本和来源身份的待验收内容。流式展示的候选内容可能在后续协作中被修订。

**验收条件（Acceptance）**：任务对产物预先定义的通过要求。

**评价（Evaluation）**：针对候选产物给出的检查结论、证据、缺陷与后续动作。评价可以要求修订、补充证据或人工介入。

**分层评价（Layered Evaluation）**：同一候选的确定性检查与 critic 判断分别保存；前者通过并不保证后者通过，两者都不替代最终业务验收。

**宿主反馈（Host Feedback）**：针对已终态任务的具体产物，由宿主明确提交的业务验收或 critic 判断正确性记录。按反馈身份幂等保存；模型、版本和角色归因来自实际调用证据，不平均分摊给所有尝试。

**指标快照（Metrics Snapshot）**：同一数据库读事务中汇总的反馈样本、调用费用/耗时/状态和任务终态。业务质量按模型版本、类型、角色、验收版本及反馈种类隔离；未知费用和未知状态保持可见。新任务固定所用反馈版本，后续反馈不改变运行中任务。

**任务结果（Task Result）**：任务结束时的状态、产物和费用信息。任务执行成功、产物通过验收与费用全部核实是不同维度。

## 预算与核实

**任务预算（Budget）**：任务可用于调用准入的费用额度，与调用次数和截止时间共同限制协作。模型与工具共享该额度。

**预留费用（Reserved Cost）**：为已准入调用占用、尚未完成实际费用结算的额度。
_避免混用_：实际支出、可用余额。

**已结算费用（Settled Cost）**：已根据已知用量或宿主报告记入账本的实际费用。即使费用超出预留，已发生的费用仍需反映在账本中。

**未知费用（Unresolved Cost）**：调用可能已发生，但实际费用尚未核实的状态。取消或失败不能据此认定实际费用为零。

**对账（Reconciliation）**：依据外部证据核实未知费用并更新账本的过程。
_避免混用_：重新调用、自动重放。

**引擎排空（Engine Drain）**：停止接收新任务后，等待已接纳任务退出执行并完成其费用处理与终态写入的过程。排空可以留下待对账费用，但不能以取消信号已发出作为完成依据。
_避免混用_：全部费用已核实、远端副作用已撤销。

## 开发期数据生命周期

开发期数据库只使用当前 schema；结构不匹配时停止打开，由宿主备份并显式选择新路径重建，
不维护跨版本自动迁移。模拟数据可清理，真实费用和未结算证据保留到对账完成，
复现与评测样本选择性归档。重建数据库不等于完成对账，也不会撤销远端副作用。

## 开发协作术语

**开发任务路由（Task Workflow Routing）**：根据用户要求选择调查、设计、实现等开发工作流的过程，作用于仓库工作。
_避免混用_：模型运行时路由。

**开发工作流（Workflow）**：具有输入、执行步骤和完成条件的仓库工作过程。一个开发任务可以按依赖关系组合多个工作流。

**技术图（Technical Diagram）**：对模块关系、调用过程或状态转换的可视化表达。当前实现图以源码证据为依据；设计图表达目标行为，需要明确其尚未实现的部分。技术图不替代领域定义或接口契约。

当前技术图见 [架构与关闭流程索引](docs/diagrams/README.md)。图中“宿主”“模型路由”“引擎排空”等术语遵循本文；概念变化时核对相应图。archify 的生成、验证与维护步骤统一见 [技术图工作流](.agent/workflows/diagram.md)，维护触发条件见 [AGENTS.md](AGENTS.md)。
